# DIAGNOSTIC: Check data sizes
print(f"🔍 DATA SIZE DIAGNOSTIC:")
print(f"original_data shape: {original_data.shape}")
print(f"processed_data shape: {processed_data.shape}")
print(f"VAE latent_dim: {vae_model.latent_dim}")
print(f"VAE input_dim: {vae_model.input_dim}")
print(f"num_synthetic requested: {len(original_data)}")

# Check what the VAE actually generates
test_generation = vae_model.generate(10)  # Test with small sample
print(f"VAE test generation shape: {test_generation.shape}")
